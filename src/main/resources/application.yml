spring:
    application:
        name: LightJason - Example Miner

    session:
        store-type: redis
        redis:
            flush-mode=on-save: on-save
            namespace: spring:session

    redis:
        host: localhost
        port: 6379

server:
    servlet:
        session:
            timeout: 10800
